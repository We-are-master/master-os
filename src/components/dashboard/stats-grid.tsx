"use client";

import { OverviewExecutiveBundle } from "@/components/dashboard/overview-executive-bundle";

/** Overview KPI pack: executive metrics, tier track, top partner & accounts, cashflow — all respect dashboard date range. */
export function StatsGrid() {
  return <OverviewExecutiveBundle />;
}
