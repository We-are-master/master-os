/** Select value used to show the inline “create account” panel (must never be sent as `source_account_id` to the API). */
export const CREATE_LINKED_ACCOUNT_OPTION = "__create_new_account__" as const;
